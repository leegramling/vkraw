#pragma once

#include "core/runtime/VkContext.h"

namespace vkraw {

using VkContext = core::runtime::VkContext;
constexpr int kMaxFramesInFlight = core::runtime::kMaxFramesInFlight;

} // namespace vkraw
