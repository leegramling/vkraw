# Natural Docs Project Directory

This directory is the Natural Docs project folder used with:

- `docs/build_docs.sh`
- `docs/build_docs.bat`

Natural Docs stores generated metadata/state files here after the first run.

Planned follow-up:

- Add comment templates for classes/methods in `src/`.
- Validate Mermaid blocks in generated HTML output.
